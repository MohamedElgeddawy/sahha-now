/*export function ProductSpecs() {
  // Temporary data - replace with actual product specifications
  const specs = [
    { label: "الماركة", value: "Brand Name" },
    { label: "الموديل", value: "Model XYZ" },
    { label: "المقاسات المتوفرة", value: "S, M, L, XL, XXL" },
    { label: "الألوان المتوفرة", value: "أحمر، أزرق، أخضر" },
    { label: "الخامة", value: "100% قطن" },
    { label: "بلد المنشأ", value: "مصر" },
  ];

  return (
    <div className="space-y-4">
      <table className="w-full">
        <tbody>
          {specs.map((spec, index) => (
            <tr
              key={spec.label}
              className={index % 2 === 0 ? "bg-gray-50" : "bg-white"}
            >
              <td className="py-3 px-4 font-medium text-gray-900">
                {spec.label}
              </td>
              <td className="py-3 px-4 text-gray-700">{spec.value}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}*/
